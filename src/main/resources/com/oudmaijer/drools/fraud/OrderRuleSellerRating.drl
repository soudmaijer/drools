package com.oudmaijer.drools

import com.oudmaijer.drools.fraud.model.*

global Errors errors

rule "Seller rating rule"
    when
        $order : Order()
        Seller( rating < 5 ) from $order.seller
    then
        errors.add("Seller rating to low, needs to be manually approved: "+ $order);
end